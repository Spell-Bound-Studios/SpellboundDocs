# Boundary Overrides

Boundary Overrides is an asset to override the initial data and any terraforming changes at the boundaries of the Volume. This ensures that the mesh will close properly, for example if&#x20;
